import { OrderByIdDescPipe } from './order-by-id-desc.pipe';

describe('OrderByIdDescPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderByIdDescPipe();
    expect(pipe).toBeTruthy();
  });
});
